import { ExperimentDirective } from './experiment.directive';

describe('ExperimentDirective', () => {
  it('should create an instance', () => {
    const directive = new ExperimentDirective();
    expect(directive).toBeTruthy();
  });
});
