package fr.maif.izanami.units

import org.scalatest.funsuite.AnyFunSuite

class ContextTest extends AnyFunSuite {
  test("foo.bar should be ascendantof foo.bar.bar") {
    assert(Set.empty.size == 0)
  }
}
